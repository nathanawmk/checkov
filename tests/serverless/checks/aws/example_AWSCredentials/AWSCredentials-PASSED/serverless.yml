service: my-sls-service

provider:
  name: aws
  runtime: python3.7
  stackName: lambda-${self:service.name}
  tag: ${opt:tag}
  environment:
    TABLE_NAME: "mytable"
    BUCKET_NAME: "mybucket"
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:PutObject"
        - "s3:GetObject"
      Resource:
        - "arn:aws:s3:::somebucket/*"

functions:
  myFunc:
    environment:
      FUNC_NAME: "myfunc"
    name: myFunc
    tags:
      RESOURCE: lambda
      PUBLIC: false
      yor_trace: 45fcf09d-eba8-4615-adeb-f228d0234e4f
    iamRoleStatements:
      - Effect: Allow
        Action:
          - "lambda:InvokeFunction"
        Resource:
          - "arn:aws:lambda:#{AWS::Region}:#{AWS::AccountId}:function:invokedLambda"
    handler: Handler.handle
    timeout: 600
    memorySize: 320